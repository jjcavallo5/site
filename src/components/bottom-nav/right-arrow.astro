---
import { cn } from "@/lib/utils";
interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<span
  class={cn(
    "border-t-bg border-t-[16px] border-b-bg border-b-[16px] border-l-[16px] border-l-bg",
    className,
  )}></span>
