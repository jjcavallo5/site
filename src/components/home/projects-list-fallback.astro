<ul>
  {
    Array.from({ length: 3 }).map(() => (
      <li class="flex flex-row justify-between items-center my-4 gap-4">
        <div class="flex flex-col gap-2 w-full">
          <div class="h-[1.125rem] w-36 bg-dark-gray rounded animate-pulse" />
          <div class="h-[1rem] w-full max-w-80 bg-dark-gray/60 rounded animate-pulse" />
        </div>
        <div class="h-[1rem] w-6 bg-dark-gray/40 rounded animate-pulse" />
      </li>
    ))
  }
</ul>
